# coreset-GA
We search for high-value training subsets of different sizes, but since we can’t afford to train a model for every GA candidate, we use a proxy fitness combining balance, difficulty, and diversity to evolve strong coresets efficiently.
